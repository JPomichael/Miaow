@{
    var hotelVouchData = Miaow.Presentation.Union.MiaowPreUnionEngine.Current.Resolve<
        Miaow.Service.Union.Service.IHeadVouchDataService>();
}
<span class="imgLink"></span><span class="textLink">
    @{
        IQueryable<Miaow.Application.Union.Dto.HotelBottomLinkDto> hl = hotelVouchData.GetHotelBottomLinks();
        if (hl != null)
        {
            foreach (var item in hl)
            {
        <a href="@(item.Url)" target="_blank" class="aText" title="@(item.Title)">@(item.Title)</a>
            }
        }
    }
</span>